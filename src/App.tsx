import Login from './pages/Login';

// Routing + auth are wired in a later step. For now the app renders the login
// screen so the design system and build can be verified end-to-end.
export default function App() {
  return <Login />;
}
